name: ext/dataverse CI

on:
  push:
    branches: [master, feat/dataverse-adapter]
    paths:
      - 'ext/dataverse/**'
      - '.github/workflows/ext-dataverse-ci.yml'
  pull_request:
    branches: [master]
    paths:
      - 'ext/dataverse/**'
      - '.github/workflows/ext-dataverse-ci.yml'

permissions:
  contents: read

jobs:
  test:
    name: Test (${{ matrix.go-version }} / ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go-version: ["1.24", "1.25"]

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5
        with:
          go-version: ${{ matrix.go-version }}
          cache: false

      - name: Pin toolchain to installed Go
        run: go env -w GOTOOLCHAIN=local

      - name: Test ext/dataverse
        env:
          GOWORK: "off"
        run: cd ext/dataverse && go test -v -race -count=1 ./...
        shell: bash

  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5
        with:
          go-version: "1.24"
          cache: false

      - name: Pin toolchain
        run: go env -w GOTOOLCHAIN=local

      - name: golangci-lint (ext/dataverse)
        uses: golangci/golangci-lint-action@4afd733a84b1f43292c63897423277bb7f4313a9 # v6
        env:
          GOWORK: "off"
        with:
          version: latest
          working-directory: ext/dataverse
          args: --timeout=3m ./...

  vet:
    name: Vet
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5
        with:
          go-version: "1.24"
          cache: false

      - name: Pin toolchain
        run: go env -w GOTOOLCHAIN=local

      - name: go vet (ext/dataverse)
        env:
          GOWORK: "off"
        run: cd ext/dataverse && go vet ./...
